type User {
	name: String! @length(min: 2, max: 20)
  email: String! @unique
	projects: [Project]
}

type Project {
	title: String @length(min: 3)
	notes: String
	complete: Boolean!
  createdBy: [User]
}